<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.thread">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".thread.ActAsyncTask" />
        <activity android:name=".thread.ActIntentService" />
        <activity android:name=".thread.ActThreadPriority" />
        <activity android:name=".thread.ActThreadPriority2" />
        <activity android:name=".thread.ActEmpty" />


        <service android:name=".thread.SerIntentService" />
    </application>
</manifest>